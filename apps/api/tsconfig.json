{
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
    // остальные опции оставь как есть
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "prisma/**/*", "test/**/*", "**/*.spec.ts"]
}
