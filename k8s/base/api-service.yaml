apiVersion: v1
kind: Service
metadata:
  name: afterlight-api
  namespace: afterlight-staging
spec:
  type: ClusterIP
  selector:
    app: afterlight-api
  ports:
    - name: http
      port: 8080
      targetPort: 3000
